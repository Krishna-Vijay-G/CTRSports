# dependencies
/node_modules
/.pnp
.pnp.js


# next.js
/.next/
# A build run to check a change while `next dev` holds .next — see next.config.js
/.next-verify/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — never commit real credentials
.env
.env*.local
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# pre-WebP source art, kept locally so `npm run webp -- --restore` works
/public/_originals/
/public/_decks/
/public/assets/
/docs
.vercel
.env*
